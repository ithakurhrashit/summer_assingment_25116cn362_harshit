#include<iostream>
using namespace std ;
int main() {
    string s1,s2;
    cout << "enter the first string";
    cin >> s1;
    cout << "enter the second strting";
    cin >> s2;
    if(s1.size()==s2.size()){
        // checking if frequency of all charecters is same   string s ;
     int hash1[26]={0};
      for(int i=0;i<s1.length();i++){
        hash1[int(s1[i])-97]+=1;
      }
     int hash2[26]={0};
      for(int i=0;i<s2.length();i++){
        hash2[int(s2[i])-97]+=1;
      }
      for(int i=0;i<26;i++){
        if(hash1[i]-hash2[i]!=0){
            cout << "string is not anagram";
            break ;
        }
        
      }
      
    }

}
